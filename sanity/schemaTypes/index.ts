import { investment } from "./investment";
import { siteSettings } from "./siteSettings";
import { contactRequest } from "./contactRequest";

// Copia este array (o su contenido) en el schemaTypes/index.ts de tu Sanity Studio.
export const schemaTypes = [investment, siteSettings, contactRequest];
